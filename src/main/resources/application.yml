server:
  servlet:
    context-path: /api/v1
  port: 8096

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/facturacion?createDatabaseIfNotExist=true
    username: root
    password: masterkey

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
